<h1>Prazos</h1>
<% if current_user.company? %>
<ul>
  <% if @time_delivery.errors.any? %>
    <% @time_delivery.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  <% end %>
</ul>

<%= form_with model: [@shipping_company, @time_delivery] do |p| %>

     <div>
       <%= p.label :min_distance, "Distancia mínima: " %>
       <%= p.number_field :min_distance %> em KM
     </div>

     <div>
        <%= p.label :max_distance, "Distancia maxíma" %> 
        <%= p.number_field :max_distance %> em KM
      </div>

      <div>
        <%= p.label :time, "Prazo de entrega" %></strong>
        <%= p.number_field :time %> em Dias
      </div>

      <div>
          <%= p.submit "Enviar" %>
      </div>
<% end %>
<% else %>
Apenas Transportadoras podem criar Prazos de entrega.
<% end %>      
      <div>
        <%= link_to "Voltar", shipping_company_time_deliveries_path(@shipping_company) %>
      </div>